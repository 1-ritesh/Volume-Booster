<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VolumeBooster">

        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Music Player Activity -->
        <activity
            android:name=".activities.MusicPlayerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Music Service -->
        <service
            android:name=".services.MusicService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
        </service>

        <!-- Volume Booster Service -->
        <service
            android:name=".services.VolumeBoosterService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Boot Receiver -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Notification Control Receiver -->
        <receiver
            android:name=".receivers.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.volumebooster.ACTION_VOLUME_UP" />
                <action android:name="com.volumebooster.ACTION_VOLUME_DOWN" />
                <action android:name="com.volumebooster.ACTION_TOGGLE_BOOST" />
                <action android:name="com.volumebooster.ACTION_PLAY_PAUSE" />
                <action android:name="com.volumebooster.ACTION_NEXT" />
                <action android:name="com.volumebooster.ACTION_PREV" />
            </intent-filter>
        </receiver>

        <!-- Home Screen Widget -->
        <receiver
            android:name=".receivers.VolumeWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/volume_widget_info" />
        </receiver>

    </application>
</manifest>
